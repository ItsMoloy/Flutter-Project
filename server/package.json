{
  "name": "image-proxy",
  "version": "1.0.0",
  "description": "Simple image proxy for development: fetches an image and returns it with permissive CORS headers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
